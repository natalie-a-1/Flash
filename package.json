{
  "name": "flash",
  "version": "1.0.0",
  "description": "Flash blockchain project",
  "main": "index.js",
  "scripts": {
    "test": "truffle test",
    "test:fork": "truffle test --network development_fork",
    "compile": "truffle compile",
    "migrate": "truffle migrate",
    "migrate:sepolia": "truffle migrate --network sepolia",
    "dev": "truffle develop",
    "ganache": "ganache-cli",
    "copy-contracts": "node copy-contracts.js",
    "postmigrate": "npm run copy-contracts",
    "format": "prettier --write .",
    "check:format": "prettier --check ."
  },
  "dependencies": {
    "@truffle/hdwallet-provider": "^2.1.15",
    "dotenv": "^16.3.1",
    "web3": "^1.10.0"
  },
  "devDependencies": {
    "@aave/core-v3": "^1.19.3",
    "@openzeppelin/contracts": "^5.3.0",
    "@openzeppelin/test-helpers": "^0.5.16",
    "ganache": "^7.9.2",
    "prettier": "^3.0.0",
    "truffle": "^5.11.5"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
